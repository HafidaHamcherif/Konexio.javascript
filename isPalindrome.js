function isPalindrom(str){

}

var pal= 'never odd or even';
var arr = pal.split('');
var result = '';
console.log(arr);
for(i=0; i< arr.length;i++){
    if(result = arr[i] + result){
        return (true);
    }else {
        return (false);
    }
}
